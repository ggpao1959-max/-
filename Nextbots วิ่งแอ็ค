-- [[ Nico's Menu x UI น้องเปา (Compact Edition: 2 Functions) ]] --
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- ป้องกันเปิดซ้ำซ้อน
if playerGui:FindFirstChild("MyCustomUI") then
    playerGui.MyCustomUI:Destroy()
end

-- 1. สร้าง ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "MyCustomUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

-- 2. สร้าง Frame (หน้าต่างหลัก - ปรับขนาดให้เล็กลงกะทัดรัด)
local frame = Instance.new("Frame")
frame.Name = "MainFrame"
frame.Size = UDim2.new(0, 420, 0, 225)
frame.Position = UDim2.new(0.5, -210, 0.5, -112)
frame.BackgroundColor3 = Color3.fromRGB(245, 247, 250)
frame.BorderSizePixel = 0
frame.Active = true
frame.Parent = screenGui

local uiCorner = Instance.new("UICorner")
uiCorner.CornerRadius = UDim.new(0, 16)
uiCorner.Parent = frame

-- 3. แถบหัวข้อด้านบน (Header Bar)
local headerBar = Instance.new("Frame")
headerBar.Name = "HeaderBar"
headerBar.Size = UDim2.new(1, 0, 0, 45)
headerBar.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
headerBar.BorderSizePixel = 0
headerBar.Parent = frame

local headerCorner = Instance.new("UICorner")
headerCorner.CornerRadius = UDim.new(0, 16)
headerCorner.Parent = headerBar

-- 4. ข้อความชื่อ UI
local titleLabel = Instance.new("TextLabel")
titleLabel.Name = "TitleLabel"
titleLabel.Size = UDim2.new(1, -50, 1, 0)
titleLabel.Position = UDim2.new(0, 15, 0, 0)
titleLabel.Text = "🥛 UI น้องเปา [Compact 2-Func]"
titleLabel.TextColor3 = Color3.fromRGB(40, 40, 40)
titleLabel.TextSize = 16
titleLabel.Font = Enum.Font.FredokaOne
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.BackgroundTransparency = 1
titleLabel.Parent = headerBar

-- ปุ่มปิดสคริปต์ (ปุ่ม X)
local closeButton = Instance.new("TextButton")
closeButton.Name = "CloseButton"
closeButton.Size = UDim2.new(0, 30, 0, 30)
closeButton.Position = UDim2.new(1, -38, 0.5, -15)
closeButton.BackgroundColor3 = Color3.fromRGB(240, 100, 100)
closeButton.Text = "✕"
closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
closeButton.TextSize = 14
closeButton.Font = Enum.Font.FredokaOne
closeButton.BorderSizePixel = 0
closeButton.Parent = headerBar

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(1, 0)
closeCorner.Parent = closeButton

closeButton.Activated:Connect(function()
    screenGui:Destroy()
end)

----------------------------------------------------
-- ส่วนเนื้อหาภายใน UI (จัดวางพอดี 2 ฟังก์ชัน)
----------------------------------------------------
local contentHolder = Instance.new("Frame")
contentHolder.Size = UDim2.new(1, -20, 1, -55)
contentHolder.Position = UDim2.new(0, 10, 0, 50)
contentHolder.BackgroundTransparency = 1
contentHolder.Parent = frame

-- [[ 1. Auto-Bhop ]] --
local autoBhopBtn = Instance.new("TextButton")
autoBhopBtn.Size = UDim2.new(1, 0, 0, 38)
autoBhopBtn.Position = UDim2.new(0, 0, 0, 0)
autoBhopBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
autoBhopBtn.Text = "   🐰 Auto-Bhop (กระโดดต่อเนื่อง)"
autoBhopBtn.TextColor3 = Color3.fromRGB(80, 80, 80)
autoBhopBtn.TextSize = 14
autoBhopBtn.Font = Enum.Font.GothamBold
autoBhopBtn.TextXAlignment = Enum.TextXAlignment.Left
autoBhopBtn.Parent = contentHolder

local bhopCorner = Instance.new("UICorner")
bhopCorner.CornerRadius = UDim.new(0, 10)
bhopCorner.Parent = autoBhopBtn

local bhopStatus = Instance.new("TextLabel")
bhopStatus.Size = UDim2.new(0, 70, 1, 0)
bhopStatus.Position = UDim2.new(1, -80, 0, 0)
bhopStatus.BackgroundTransparency = 1
bhopStatus.Text = "[ OFF ]"
bhopStatus.TextColor3 = Color3.fromRGB(200, 60, 60)
bhopStatus.TextSize = 13
bhopStatus.Font = Enum.Font.GothamBold
bhopStatus.Parent = autoBhopBtn

local autoBhopActive = false
autoBhopBtn.MouseButton1Click:Connect(function()
    autoBhopActive = not autoBhopActive
    if autoBhopActive then
        bhopStatus.Text = "[ ON ]"
        bhopStatus.TextColor3 = Color3.fromRGB(46, 204, 113)
        autoBhopBtn.BackgroundColor3 = Color3.fromRGB(235, 255, 240)
    else
        bhopStatus.Text = "[ OFF ]"
        bhopStatus.TextColor3 = Color3.fromRGB(200, 60, 60)
        autoBhopBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    end
end)

RunService.RenderStepped:Connect(function()
    if autoBhopActive then
        local char = player.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum and hum.FloorMaterial ~= Enum.Material.Air then
                hum:ChangeState(Enum.HumanoidStateType.Jumping)
            end
        end
    end
end)

-- [[ 2. Speed Multiplier (10 ระดับ) ]] --
local speedContainer = Instance.new("Frame")
speedContainer.Size = UDim2.new(1, 0, 0, 80)
speedContainer.Position = UDim2.new(0, 0, 0, 46)
speedContainer.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
speedContainer.Parent = contentHolder

local speedCorner = Instance.new("UICorner")
speedCorner.CornerRadius = UDim.new(0, 10)
speedCorner.Parent = speedContainer

local speedTitle = Instance.new("TextLabel")
speedTitle.Size = UDim2.new(1, -15, 0, 25)
speedTitle.Position = UDim2.new(0, 12, 0, 4)
speedTitle.BackgroundTransparency = 1
speedTitle.Text = "⚡ Speed 10 Levels (24 × ระดับ)"
speedTitle.TextColor3 = Color3.fromRGB(80, 80, 80)
speedTitle.TextSize = 13
speedTitle.Font = Enum.Font.GothamBold
speedTitle.TextXAlignment = Enum.TextXAlignment.Left
speedTitle.Parent = speedContainer

local speedMultiplierActive = false
local currentLevel = 1

-- ปุ่มเปิด-ปิด Speed
local speedToggleBtn = Instance.new("TextButton")
speedToggleBtn.Size = UDim2.new(0, 80, 0, 32)
speedToggleBtn.Position = UDim2.new(1, -90, 0, 38)
speedToggleBtn.BackgroundColor3 = Color3.fromRGB(255, 235, 235)
speedToggleBtn.Text = "OFF"
speedToggleBtn.TextColor3 = Color3.fromRGB(200, 60, 60)
speedToggleBtn.TextSize = 12
speedToggleBtn.Font = Enum.Font.GothamBold
speedToggleBtn.Parent = speedContainer

local speedToggleCorner = Instance.new("UICorner")
speedToggleCorner.CornerRadius = UDim.new(0, 6)
speedToggleCorner.Parent = speedToggleBtn

speedToggleBtn.MouseButton1Click:Connect(function()
    speedMultiplierActive = not speedMultiplierActive
    if speedMultiplierActive then
        speedToggleBtn.Text = "ON"
        speedToggleBtn.TextColor3 = Color3.fromRGB(46, 204, 113)
        speedToggleBtn.BackgroundColor3 = Color3.fromRGB(235, 255, 240)
    else
        speedToggleBtn.Text = "OFF"
        speedToggleBtn.TextColor3 = Color3.fromRGB(200, 60, 60)
        speedToggleBtn.BackgroundColor3 = Color3.fromRGB(255, 235, 235)
        local char = player.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then hum.WalkSpeed = 16 end
        end
    end
end)

-- ปุ่มลด (-)
local minusBtn = Instance.new("TextButton")
minusBtn.Size = UDim2.new(0, 30, 0, 32)
minusBtn.Position = UDim2.new(0, 12, 0, 38)
minusBtn.BackgroundColor3 = Color3.fromRGB(230, 230, 240)
minusBtn.Text = "-"
minusBtn.TextColor3 = Color3.fromRGB(40, 40, 40)
minusBtn.TextSize = 15
minusBtn.Font = Enum.Font.GothamBold
minusBtn.Parent = speedContainer

local minusCorner = Instance.new("UICorner")
minusCorner.CornerRadius = UDim.new(0, 6)
minusCorner.Parent = minusBtn

-- ป้ายแสดงระดับ
local valueLabel = Instance.new("TextLabel")
valueLabel.Size = UDim2.new(0, 120, 0, 32)
valueLabel.Position = UDim2.new(0, 45, 0, 38)
valueLabel.BackgroundTransparency = 1
valueLabel.Text = "24 × " .. currentLevel .. " (" .. (24 * currentLevel) .. ")"
valueLabel.TextColor3 = Color3.fromRGB(40, 40, 40)
valueLabel.TextSize = 12
valueLabel.Font = Enum.Font.GothamBold
valueLabel.Parent = speedContainer

-- ปุ่มเพิ่ม (+)
local plusBtn = Instance.new("TextButton")
plusBtn.Size = UDim2.new(0, 30, 0, 32)
plusBtn.Position = UDim2.new(0, 168, 0, 38)
plusBtn.BackgroundColor3 = Color3.fromRGB(230, 230, 240)
plusBtn.Text = "+"
plusBtn.TextColor3 = Color3.fromRGB(40, 40, 40)
plusBtn.TextSize = 15
plusBtn.Font = Enum.Font.GothamBold
plusBtn.Parent = speedContainer

local plusCorner = Instance.new("UICorner")
plusCorner.CornerRadius = UDim.new(0, 6)
plusCorner.Parent = plusBtn

minusBtn.MouseButton1Click:Connect(function()
    if currentLevel > 1 then
        currentLevel = currentLevel - 1
        valueLabel.Text = "24 × " .. currentLevel .. " (" .. (24 * currentLevel) .. ")"
    end
end)

plusBtn.MouseButton1Click:Connect(function()
    if currentLevel < 10 then
        currentLevel = currentLevel + 1
        valueLabel.Text = "24 × " .. currentLevel .. " (" .. (24 * currentLevel) .. ")"
    end
end)

RunService.RenderStepped:Connect(function()
    if speedMultiplierActive then
        local char = player.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then
                hum.WalkSpeed = 24 * currentLevel
            end
        end
    end
end)

----------------------------------------------------
-- สร้างปุ่มกลมเปิด-ปิด UI (หมุนได้ + ลากได้)
----------------------------------------------------
local toggleButton = Instance.new("TextButton")
toggleButton.Name = "ToggleButton"
toggleButton.Size = UDim2.new(0, 50, 0, 50)
toggleButton.Position = UDim2.new(0.05, 0, 0.2, 0)
toggleButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
toggleButton.Text = "🥛"
toggleButton.TextSize = 25
toggleButton.BorderSizePixel = 0
toggleButton.Parent = screenGui

local btnCorner = Instance.new("UICorner")
btnCorner.CornerRadius = UDim.new(1, 0)
btnCorner.Parent = toggleButton

local btnStroke = Instance.new("UIStroke")
btnStroke.Color = Color3.fromRGB(220, 220, 220)
btnStroke.Thickness = 2
btnStroke.Parent = toggleButton

local runConnection
runConnection = RunService.RenderStepped:Connect(function(deltaTime)
    if toggleButton and toggleButton.Parent then
        toggleButton.Rotation = (toggleButton.Rotation + (50 * deltaTime)) % 360
    else
        if runConnection then runConnection:Disconnect() end
    end
end)

-- ระบบลาก MainFrame
local function enableDrag(targetElement, moveElement)
    local dragging, dragStart, startPos
    targetElement.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = moveElement.Position
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            moveElement.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)
end
enableDrag(headerBar, frame)

-- ระบบลากปุ่มกลมเปิด-ปิด
local btnDragging, btnDragStart, btnStartPos, isMoved = false, nil, nil, false
toggleButton.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        btnDragging = true
        isMoved = false
        btnDragStart = input.Position
        btnStartPos = toggleButton.Position
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if btnDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - btnDragStart
        if math.abs(delta.X) > 5 or math.abs(delta.Y) > 5 then isMoved = true end
        toggleButton.Position = UDim2.new(btnStartPos.X.Scale, btnStartPos.X.Offset + delta.X, btnStartPos.Y.Scale, btnStartPos.Y.Offset + delta.Y)
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        btnDragging = false
    end
end)

toggleButton.Activated:Connect(function()
    if not isMoved then
        frame.Visible = not frame.Visible
    end
end)

print("🥛 UI น้องเปา [Compact 2-Func Edition] Loaded Successfully!")
